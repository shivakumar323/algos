// Topic - functions in js
/*
1. syntax: 
function fun(arg1, arg2) {
    piece of code
}

2. function namae in js should be logical

3. function name can contain :
    a. alphabets (both small and caps)
    b. numbers (0 to 9)
    c. 2 special characters ('$' and '_')

4. function name can start with :
   a. character
   b. '$'
   c. '_'

   Note : function name cannot start with number

5. function name in js should follow ccamel casing
  i.e., function name should be something like 
  a. findSum()
  b. findAlphabet()
  etc

*/